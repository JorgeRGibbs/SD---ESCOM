create database if not exists coordinador;
USE coordinador;
CREATE TABLE if not exists PLAYER(
IP varchar(30),
HORA VARCHAR(20),
SUMA INT
);
ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY 'root';
create database if not exists coordinador1;
USE coordinador1;
CREATE TABLE if not exists PLAYER(
IP varchar(30),
HORA VARCHAR(20),
SUMA INT
);
ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY 'root';
create database if not exists coordinador2;
USE coordinador2;
CREATE TABLE if not exists PLAYER(
IP varchar(30),
HORA VARCHAR(20),
SUMA INT
);
ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY 'root';
